---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sigma_tag Resource - terraform-provider-sigma"
subcategory: ""
description: |-
  Manages a Sigma version tag. The update API only accepts description; changing name or color forces replacement.
---

# sigma_tag (Resource)

Manages a Sigma version tag. The update API only accepts `description`; changing `name` or `color` forces replacement.

## Example Usage

```terraform
resource "sigma_tag" "example" {
  name        = "production"
  color       = "cyan"
  description = "Production tagged version"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `color` (String) Tag color: `cyan`, `grass`, `violet`, `plum`, `amber`, or `bronze`.
- `name` (String) Unique tag name.

### Optional

- `description` (String) Tag description.

### Read-Only

- `id` (String) Version tag ID (`versionTagId`).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/usr/bin/env sh
terraform import sigma_tag.example version-tag-id
```
