---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sigma_user_attribute Resource - terraform-provider-sigma"
subcategory: ""
description: |-
  Manages a Sigma user attribute. The API has no update endpoint, so configuration changes replace it.
---

# sigma_user_attribute (Resource)

Manages a Sigma user attribute. The API has no update endpoint, so configuration changes replace it.

## Example Usage

```terraform
resource "sigma_user_attribute" "example" {
  name          = "Region"
  description   = "Data access region"
  default_value = "global"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) User attribute name.

### Optional

- `default_value` (String) Default string value.
- `description` (String) User attribute description.

### Read-Only

- `id` (String) User attribute ID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/usr/bin/env sh
terraform import sigma_user_attribute.example user-attribute-id
```
