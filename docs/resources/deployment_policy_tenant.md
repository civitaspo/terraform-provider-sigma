---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sigma_deployment_policy_tenant Resource - terraform-provider-sigma"
subcategory: ""
description: |-
  Attaches exactly one tenant organization to an existing Sigma deployment policy. This resource never creates the parent policy. This resource uses a Sigma Beta API and may change without notice.
---

# sigma_deployment_policy_tenant (Resource)

Attaches exactly one tenant organization to an existing Sigma deployment policy. This resource never creates the parent policy. This resource uses a Sigma Beta API and may change without notice.

## Example Usage

```terraform
resource "sigma_deployment_policy_tenant" "example" {
  deployment_policy_id = "deployment-policy-id"
  tenant_id            = "tenant-organization-id"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `deployment_policy_id` (String) Existing deployment policy ID.
- `tenant_id` (String) Tenant organization ID to attach.

### Read-Only

- `id` (String) Composite ID `deploymentPolicyId/tenantId`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/usr/bin/env sh
terraform import sigma_deployment_policy_tenant.example deployment-policy-id/tenant-organization-id
```
