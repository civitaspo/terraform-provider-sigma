---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "sigma_connection_grant Resource - terraform-provider-sigma"
subcategory: ""
description: |-
  Manages a permission grant on a Sigma connection.
---

# sigma_connection_grant (Resource)

Manages a permission grant on a Sigma connection.

## Example Usage

```terraform
resource "sigma_connection_grant" "example" {
  connection_id = sigma_connection.example.id
  team_id       = "team-id"
  permission    = "usage"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `connection_id` (String) Connection ID.
- `permission` (String) Permission: `usage` or `annotate`.

### Optional

- `member_id` (String) Member ID. Exactly one of `member_id` or `team_id` is required.
- `team_id` (String) Team ID. Exactly one of `member_id` or `team_id` is required.

### Read-Only

- `connection_path_id` (String) Connection path ID; only used by `sigma_connection_path_grant`.
- `id` (String) Grant ID.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/usr/bin/env sh
terraform import sigma_connection_grant.example connection-id/grant-id
```
